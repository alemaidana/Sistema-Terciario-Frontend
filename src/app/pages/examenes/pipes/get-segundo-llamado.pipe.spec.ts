import { GetSegundoLLamadoPipe } from './get-segundo-llamado.pipe';

describe('GetSegundoLLamadoPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSegundoLLamadoPipe();
    expect(pipe).toBeTruthy();
  });
});
