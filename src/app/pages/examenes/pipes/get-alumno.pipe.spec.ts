import { GetAlumnoPipe } from './get-alumno.pipe';

describe('GetAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
