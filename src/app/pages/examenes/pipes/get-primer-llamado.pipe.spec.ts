import { GetPrimerLLamadoPipe } from './get-primer-llamado.pipe';

describe('GetPrimerLLamadoPipe', () => {
  it('create an instance', () => {
    const pipe = new GetPrimerLLamadoPipe();
    expect(pipe).toBeTruthy();
  });
});
