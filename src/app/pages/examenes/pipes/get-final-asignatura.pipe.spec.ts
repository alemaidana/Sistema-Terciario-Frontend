import { GetFinalAsignaturaPipe } from './get-final-asignatura.pipe';

describe('GetFinalAsignaturaPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFinalAsignaturaPipe();
    expect(pipe).toBeTruthy();
  });
});
