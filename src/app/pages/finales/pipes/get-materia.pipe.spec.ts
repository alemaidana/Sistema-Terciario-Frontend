import { GetMateriaPipe } from './get-materia.pipe';

describe('GetMateriaPipe', () => {
  it('create an instance', () => {
    const pipe = new GetMateriaPipe();
    expect(pipe).toBeTruthy();
  });
});
