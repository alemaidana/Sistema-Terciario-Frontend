import { GetMatriculaDocentePipe } from './get-matricula-docente.pipe';

describe('GetMatriculaDocentePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMatriculaDocentePipe();
    expect(pipe).toBeTruthy();
  });
});
