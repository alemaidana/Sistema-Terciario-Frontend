import { GetCarreraPipe } from './get-carrera.pipe';

describe('GetCarreraPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCarreraPipe();
    expect(pipe).toBeTruthy();
  });
});
