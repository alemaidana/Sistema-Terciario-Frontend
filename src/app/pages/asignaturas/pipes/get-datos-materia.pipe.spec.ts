import { GetDatosMateriaPipe } from './get-datos-materia.pipe';

describe('GetDatosMateriaPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDatosMateriaPipe();
    expect(pipe).toBeTruthy();
  });
});
