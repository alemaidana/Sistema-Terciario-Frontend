import { GetDatosDocentePipe } from './get-datos-docente.pipe';

describe('GetDatosDocentePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDatosDocentePipe();
    expect(pipe).toBeTruthy();
  });
});
