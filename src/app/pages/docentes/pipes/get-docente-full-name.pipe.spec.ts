import { GetDocenteFullNamePipe } from './get-docente-full-name.pipe';

describe('GetDocenteFullNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDocenteFullNamePipe();
    expect(pipe).toBeTruthy();
  });
});
