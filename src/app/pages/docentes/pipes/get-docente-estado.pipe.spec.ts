import { GetDocenteEstadoPipe } from './get-docente-estado.pipe';

describe('GetDocenteEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDocenteEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
